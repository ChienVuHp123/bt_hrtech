#include <stdio.h>
#include <math.h>
int main () {
	int n, m = 1, i;
	scanf("%d", &n);
	int A[n];
	for (i = 0; i < n; i++)
	{
		scanf("%d", &A[i]);
		if (i>0)
		{
		    if (A[i]!=A[i-1])
		    {
			    m = m+1;
		    }
		}    
	}
	printf("%d", m);
	return 0;
}
