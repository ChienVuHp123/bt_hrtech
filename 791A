#include <stdio.h>
#include <math.h>
int main() {
    int Limak, Bob;
    scanf("%d %d", &Limak, &Bob);
    int i = 0;
    while( Limak <= Bob)
    {
        Limak = Limak*3;
        Bob =  Bob*2;
        i++;
    }
    printf("%d", i);
    return 0;
}
